
library_array = []

running_libraries = []
def get_current_day_value:


def get_next_books(library_array,days_left):
    #for each library find its value per day
    for library in library_array:
        value_per_day =


def find_max_value(library_array, days):
    MAX_VALUE = 0
    for days_left in range(0,days,-1):
        #find the value gained from today

        #find the next books that are


        #-1 from the libraries sign up process and if they are able run then put them in the running list


